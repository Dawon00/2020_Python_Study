minute = int(input('additional minute: '))
message = int(input('additional text message: '))

if minute == 0 and message == 0:
    print('추가 비용이 없습니다.','$', "%0.2f" % (105 * (15 + 0.44)))
else:
    print('총 비용은','$', "%0.2f" % (105 * ( 15 + minute * 0.25 + message * 0.15 + 0.44)))
