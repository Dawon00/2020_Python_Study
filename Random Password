#암호의 길이는 7~10자 (랜덤)
#암호의 내용도 ASCII코드 기준 33~126까지(랜덤)

def password():
	import random
	length = random.randrange(7,11)
	for i in range(length):
		print(chr(random.randrange(33,127)),end='')

password()
print('\n')
