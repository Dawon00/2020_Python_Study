approximations = 3
for i in range(1, 16): #i를 1부터 15까지
    term = 4 / ((i * 2) * ((i * 2) + 1) * ((i * 2) + 2)) #분수하나를 한 term으로 생각
    if i % 2 == 0: #i가 짝수면
        approximations -= term 
    else: #i가 홀수면
        approximations += term
print(approximations)
​
